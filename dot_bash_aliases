run() { make $1 && ./$@; }
alias gdb="gdb -q"

submit() {
    local name=$(basename $PWD)
    mkdir -p ~/CJ-ckx/$name
    cp sol.cpp ~/CJ-ckx/$name.cpp
    cp sol.cpp ~/CJ-ckx/$name/$name.cpp
}

export PATH="$PATH:~/apps"
alias cz="chezmoi"